<%= @product.name %>
<%= @product.classification %>
<%= @product.purchase_price %>
<%= @product.selling_price %>
<%= link_to 'Edit product', edit_product_path(@product) %>
<%= link_to 'Delete product', product_path(@product), data: {turbo_method: :delete, turbo_confirm: 'Are you sure to remove the product?'} %>